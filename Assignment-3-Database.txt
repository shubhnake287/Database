Assignment-3

1)Does ANSI recognize the data type DATE?
Ans:-yes. 
The ANSIstandards specify qualifiers and formats for character representations of DATETIME and INTERVAL values 
The standard qualifier for a DATETIME value is YEAR TO SECOND, and the standard format is as follows
YYYY-MM-DD HH:MM:SS

2)Which subdivision of SQL is used to insert the values in tables.?
Ans->we used DML-data manupulation language.
To insert data into table you can use normal INSERT command.
Command->insert into table(column1,column2)values(10,20).
In that way we just insert the values in the tables.